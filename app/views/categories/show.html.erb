<h1><%= @category.title %></h1>

<ul>
<% @category.items.each do |item| %>
    <li><%= item.title %> | <%= number_to_currency(item.to_frac(item.price)) %> | <%= button_to 'Add to Cart', line_items_path(item_id: item) %></li>
<% end %>
</ul>